/**
 * Problem: Names: Store the names of a few of your friends in a array called names. Print each person’s name by accessing each element in the list, one at a time.
 */

let names_12: string[] = ["Husnain", "Arsalan", "Faizan", "Riaz", "Murtaza"];

for (let i = 0; i < names_12.length; i++) {
    console.log(`Hello ${names_12[i]} ! How are you? `);
}

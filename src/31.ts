let users: string[] = ["admin", "Eric", "Robert", "Peter", "Andras"];

// check if users array is empty
if (users.length === 0) {
    console.log("We need to find some users!");
}

users = [];

if(users.length == 0 ) {
    console.log("We need to find some users!")
}